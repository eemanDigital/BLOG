import express from "express";
import { getAllPosts } from "../controller/posts_controller.js";

const router = express.Router();
// const {getAllPosts} = posts
// router.get("/test", getAllPosts);

export default router;
